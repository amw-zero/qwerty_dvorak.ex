# QwertyDvorak

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add qwerty_dvorak to your list of dependencies in `mix.exs`:

        def deps do
          [{:qwerty_dvorak, "~> 0.0.1"}]
        end

  2. Ensure qwerty_dvorak is started before your application:

        def application do
          [applications: [:qwerty_dvorak]]
        end

